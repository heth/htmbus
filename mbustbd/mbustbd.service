[Unit]
Description=M-Bus Thingsboard connection daemon
After=network.target
Before=nginx.service

[Service]
ExecStart=PYTHONNAME SERVICENAME
Type=simple
Restart=always


[Install]
WantedBy=default.target
RequiredBy=network.target
